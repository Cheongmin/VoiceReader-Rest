import json


def test_add_user_invalid_idtoken():
    # data = json.dumps({'display_name': 'test name'})
    # res = test_client.post('api/v1/users', data=data, content_type='application/json')
    #
    # result = json.loads(res.data.decode())

    assert 201 == 201
